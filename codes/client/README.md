# 드론 탐지 클라이언트

이미지를 올리고 YOLO 드론 탐지 결과를 확인하는 React 웹 화면입니다. 기본 API 주소는 `http://localhost:8001`입니다.

```bash
npm install
npm run dev
```

브라우저에서 `http://localhost:5173`으로 접속합니다. API 주소를 바꾸려면 `.env.example`을 `.env`로 복사하고 `VITE_API_BASE_URL`을 수정합니다.

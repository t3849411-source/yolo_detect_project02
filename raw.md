# 드론 객체 탐지 웹 애플리케이션

사진과 모바일 카메라 영상에서 드론 기체를 탐지하고 분석 결과를 시각화하는 YOLO 기반 웹 애플리케이션을 만든다. 사진 탐지는 REST API로 제공하고, 실시간 카메라 탐지는 WebSocket으로 프레임과 결과를 주고받는다. 완성된 클라이언트와 추론 서버는 Render에 분리 배포한다.

## 1. 데이터셋

1. 아래의 Kaggle 데이터셋 두 개를 함께 사용한다.
   - [YOLO Drone Detection Dataset](https://www.kaggle.com/datasets/muki2003/yolo-drone-detection-dataset)
   - [Drones Dataset YOLO](https://www.kaggle.com/datasets/monkeyboy999/drones-dataset-yolo) — Kaggle 표시 라이선스: CC BY 4.0
2. 탐지 클래스는 `drone` 하나이며 두 데이터셋 모두 YOLO 클래스 번호 `0`으로 통일한다.
3. 첫 번째 데이터셋은 `./images/`에 JPEG 이미지 1,359장, `./labels/`에 대응하는 YOLO 라벨 1,359개로 저장되어 있다. 원본 분할은 학습 1,012장과 검증 347장이며 실제 통합 학습에서는 파일 내용이 같은 중복 20장을 제외한다.
4. 두 번째 데이터셋은 `./datasets/monkeyboy999-drones-dataset-yolo/`에 이미지와 YOLO 라벨을 함께 저장한다. 중복 제거 후 구성은 학습 7,097장, 검증 1,525장, 테스트 1,524장으로 총 10,146장이다.
5. 두 번째 데이터셋에는 바운딩 박스 10,363개와 드론이 없는 배경 이미지 929장이 있다. 배경 이미지의 빈 라벨 파일은 정상 데이터로 취급한다.
6. 두 번째 데이터셋의 원본 10,158장 가운데 파일 내용이 같은 중복 이미지 12장을 라벨과 함께 제거했다. 그중 2쌍은 서로 다른 분할에 들어 있던 누수 데이터였다. 제거 내역은 `./datasets/monkeyboy999-drones-dataset-yolo/removed_duplicates.txt`에 기록한다.
7. 두 데이터셋 사이에는 파일 내용이 같은 이미지가 없다. 중복 제거 후 실제 학습·검증·테스트에 사용하는 고유 이미지는 총 11,485장이다.
8. 통합 학습 설정 파일은 `./datasets/combined-drone/data.yaml`을 사용한다. 구성은 학습 7,994장, 검증 1,625장, 테스트 1,866장이며 상세 내역은 같은 폴더의 `metadata.json`에 기록한다.
9. 첫 번째 데이터셋의 원본 검증 이미지와 두 번째 데이터셋의 원본 테스트 이미지는 통합 테스트 세트로 유지하며 학습에 섞지 않는다. 첫 번째 데이터셋의 중복 제거된 학습 풀 중 10%는 시드 `42`로 검증에 배정한다.

## 2. YOLO 모델

1. 기본 가중치 `yolo26n.pt`를 드론 데이터셋으로 파인튜닝한다.
2. 학습이 완료된 가중치는 `./codes/server/drone-detection-yolo26n.pt`에 저장한다.
3. 현재 저장된 가중치는 두 데이터셋의 통합 학습 세트 7,994장으로 50 epoch 학습한 모델이다. 입력 크기는 640, batch는 32이며 시드 `42`, deterministic 모드, AMP를 사용했다.
4. 독립 통합 테스트 1,866장 결과는 precision 0.977, recall 0.959, mAP50 0.971, mAP50-95 0.614이다. CPU 추론 시간은 이미지당 16.0ms이다.
5. 학습 및 평가 과정과 실행 결과는 `./drone_yolo_training.ipynb`에 기록한다.
6. 학습된 가중치가 없거나 불러올 수 없으면 서버가 다른 모델로 자동 대체하지 말고 원인을 명확하게 알린다.
7. 서버는 애플리케이션 시작 시 모델을 한 번만 로드하고 요청마다 다시 로드하지 않는다.
8. 서비스의 모든 추론은 GPU를 사용하지 않고 CPU에서 실행한다.
9. Render 배포 전에 최종 PyTorch 모델을 ONNX로 내보내고 정확도, 추론 시간, 메모리 사용량을 기존 방식과 비교한다.
10. ONNX 모델은 독립 테스트 세트에서 기존 모델과 허용 가능한 정확도 차이를 보일 때만 배포 모델로 선택한다.

## 3. 개발 환경

1. 개발 환경은 Ubuntu 24.04 WSL이다.
2. 클라이언트는 Vite, TypeScript, React를 사용한다.
3. 서버는 Python 3.12, FastAPI, Uvicorn을 사용한다.
4. 배포 환경에서는 Gunicorn을 사용하고 워커 수는 1개로 고정한다.
5. 서버 의존성은 `./codes/server/pyproject.toml`에서 관리한다.
6. 배포용 추론 서버는 학습 및 문서 처리 패키지를 제외한 최소 의존성으로 구성한다.
7. 클라이언트의 배포 API 주소는 `VITE_API_BASE_URL` 환경 변수로 주입한다.
8. 서버는 Render가 제공하는 `PORT` 환경 변수와 `0.0.0.0` 호스트에 바인딩한다.

## 4. 이미지 업로드

1. 사용자는 드래그 앤 드롭으로 이미지를 업로드할 수 있다.
2. 사용자는 버튼을 눌러 파일 탐색기에서도 이미지를 선택할 수 있다.
3. 클라이언트는 선택한 이미지의 미리보기를 제공한다.
4. 서버의 `POST /api/v1/detect`에 `multipart/form-data`의 `file` 필드로 이미지를 전송한다.
5. 서버는 JPEG, PNG, WebP, BMP를 지원하며 기본 파일 크기 제한은 10MB, 이미지 크기 제한은 4천만 픽셀이다.
6. 서버는 지원하지 않는 형식에 HTTP 415, 읽을 수 없는 이미지에 HTTP 422를 반환한다.

## 5. 실시간 카메라 탐지

1. 클라이언트의 탐지 방식을 `사진 탐지`와 `실시간 카메라` 두 화면으로 구분한다.
2. 카메라는 사용자가 시작 버튼을 누른 뒤에만 `getUserMedia()`로 권한을 요청한다.
3. 모바일에서는 후면 카메라를 우선 사용하고 전면·후면 카메라 전환 기능을 제공한다.
4. 카메라 권한 거부, 지원 장치 없음, 보안 연결이 아닌 경우를 구분하여 안내한다.
5. 브라우저는 카메라 프레임을 최대 640픽셀 입력 크기로 축소하고 JPEG 또는 WebP 바이너리로 변환한다.
6. 실시간 통신에는 `wss://<server>/api/v1/stream` WebSocket을 사용한다.
7. 클라이언트는 이전 프레임의 결과를 받은 뒤 다음 프레임을 보내 서버 요청이 누적되지 않게 한다.
8. 전송 간격은 최근 추론 시간과 네트워크 지연에 맞춰 자동 조절한다. Render 무료 배포의 초기 목표는 초당 1~3회 분석이다.
9. 서버는 각 프레임의 탐지 상자, 신뢰도, 추론 시간과 프레임 식별자를 JSON으로 반환한다.
10. 클라이언트는 카메라 영상 위의 캔버스에 탐지 상자와 신뢰도를 표시하고 현재 분석률과 지연 시간을 보여준다.
11. 브라우저 탭이 숨겨지거나 사용자가 종료 버튼을 누르면 모든 카메라 트랙과 WebSocket 연결을 즉시 종료한다.
12. 연결이 일시적으로 끊기면 지수형 대기 시간을 적용해 제한적으로 재연결하고 사용자에게 연결 상태를 표시한다.
13. 카메라 영상과 전송 프레임은 서버 파일 시스템이나 데이터베이스에 저장하지 않는다.
14. 현재 단계에서는 녹화된 동영상 파일 업로드를 범위에 포함하지 않으며, 실시간 카메라 안정화 뒤 별도 기능으로 검토한다.

## 6. 추론 결과

1. 서버는 업로드된 이미지에서 드론을 탐지한다.
2. 서버는 각 탐지 결과의 바운딩 박스 좌표와 신뢰도를 반환한다.
3. 서버는 전체 탐지 수와 추론 시간을 반환한다.
4. 클라이언트는 원본 이미지 위에 바운딩 박스와 신뢰도를 표시한다.
5. 드론이 탐지되지 않은 경우에도 정상 응답을 반환하고, 클라이언트는 탐지 결과가 없음을 명확하게 보여준다.
6. `GET /health`는 모델 이름, SHA-256, 실행 장치와 클래스 정보를 반환한다.

## 7. 결과 시각화

1. Chart.js를 사용하여 YOLO 학습 및 추론 결과를 보여준다.
2. 독립 테스트의 precision, recall, mAP50, mAP50-95를 비교하는 차트를 제공한다.
3. 사진 또는 현재 카메라 프레임의 탐지별 신뢰도를 보여준다.
4. 탐지 상자의 이미지 점유율을 비교하여 객체 크기를 보여준다.
5. 이미지 해석, 모델 추론, 네트워크 왕복과 전체 처리 시간을 구분하여 보여준다.
6. 단일 클래스 데이터셋이라는 특성을 고려하여 서로 같은 정보만 반복하는 차트를 만들지 않는다.

## 8. 화면 및 검증

1. 업로드 전, 분석 중, 분석 성공, 탐지 결과 없음, 오류 상태를 각각 구분하여 보여준다.
2. 모바일, 태블릿, 데스크톱에서 사용할 수 있는 반응형 화면을 만든다.
3. Playwright를 사용해 주요 화면 크기에서 업로드 흐름과 반응형 레이아웃을 확인한다.
4. 카메라 대기, 권한 요청, 스트리밍, 일시 정지, 연결 복구와 권한 거부 상태를 구분한다.
5. 실제 Android와 iOS 브라우저에서 후면 카메라, 화면 회전, 백그라운드 전환과 권한 흐름을 확인한다.
6. 카메라를 자동으로 켜지 않고 사용자의 명시적인 동작으로만 시작한다.

## 9. Render 배포

1. React 클라이언트는 Render Static Site로 배포하고 `codes/client/dist`를 게시한다.
2. FastAPI 추론 서버는 Render Web Service로 배포하며 HTTP와 WebSocket을 동일한 공개 포트에서 제공한다.
3. 공개 통신에는 HTTPS와 WSS만 사용한다.
4. 서버의 `CORS_ORIGINS`에는 실제 Static Site 주소만 등록한다.
5. `GET /health`를 Render 상태 확인 경로로 사용한다.
6. 무료 Web Service의 0.1 CPU, 512MB 메모리와 유휴 종료 제약을 고려하여 ONNX Runtime 기반 서버를 우선 검증한다.
7. 무료 환경에서 메모리 또는 지연 목표를 만족하지 못하면 1 CPU, 2GB 이상의 인스턴스를 사용한다.
8. 무료 서버가 다시 시작되는 동안 클라이언트는 준비 중 상태를 표시하고 제한 시간 이후 재시도 방법을 안내한다.
9. WebSocket은 배포나 서버 재시작으로 종료될 수 있으므로 클라이언트 재연결을 전제로 설계한다.
10. 배포 설정과 환경 변수 예시는 저장소에 포함하되 비밀 값은 Git에 저장하지 않는다.

## 10. 저장소와 포트폴리오 문서

1. 루트 `README.md`에 프로젝트 목적, 실행 화면, 구조, 데이터 정제 과정, 모델 성능, 실행법, API와 로드맵을 기록한다.
2. 원본 데이터셋, 가상 환경, 학습 실행 폴더와 빌드 결과는 Git에서 제외한다.
3. 서비스 가중치는 체크섬을 함께 제공하고 Git LFS 또는 릴리스 파일로 관리할 수 있게 구성한다.
4. 작은 테스트 이미지는 자동 검사 전용 fixture로 따로 보관한다.
5. GitHub Actions에서 클라이언트 타입 검사·빌드·Playwright 검사와 서버 스모크 테스트를 실행한다.
6. README에는 현재 구현과 계획된 기능을 구분하여 아직 구현하지 않은 기능을 완료된 것으로 표시하지 않는다.
